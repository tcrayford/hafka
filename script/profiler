#!/bin/bash

source script/test_helper

main() {
  (cd src && ghc --make -fforce-recomp -rtsopts -threaded -O2 -o ./profiled Profiling/Main.hs)
  (start_server)
  (cd src && ./profiled +RTS -N4)
  kill_server
}

main
